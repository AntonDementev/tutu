<p id="notice"><%= notice %></p>

<p>
  <strong>Number:</strong>
  <%= @train.number %>
</p>

<p>
  <% if @train.route.present?%>
    Назначен маршрут: <%= @train.route.title  %>
  <% else %>
    Маршрут не назначен
  <% end %>
</p>

<p>
  <% if @train.waggons.present?%>
    Количество вагонов: <%= @train.waggons.size  %>
  <% else %>
    Вагонов нет
  <% end %>
</p>


<p>
<ul>
Вагоны:
<% if  @train.head_to_tail %>
  <% @train.waggons.order(:number).each_with_index do |waggon, index| %>
    <li><%= link_to "#{waggon.number} (#{waggon.type})", waggon.becomes(Waggon) %></li>
  <% end %>
<% else %>
  <% @train.waggons.order("number DESC").each_with_index do |waggon, index| %>
    <li><%= link_to "#{waggon.number} (#{waggon.type})", waggon.becomes(Waggon) %></li>
  <% end %>
<% end %>  
</ul></p>

<%= link_to 'Edit', edit_train_path(@train) %> |
<%= link_to 'Back', trains_path %>
